import 'package:flutter/material.dart';

class SuperGroupPage extends StatelessWidget {
  const SuperGroupPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("SuperGroup Page"),
    );
  }
}
